server:
  port: 8001

spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource     #当前数据源的操作类型
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_cloud2020?serverTimezone=UTC&characterEncoding=utf-8&useSSL=false
    username: root
    password: huidong123

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: pers.huidong.springcloud.entities   #所有Entity别名类所在包